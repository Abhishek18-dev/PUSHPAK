# DEPENDENCY MANIFEST

## Approved Core Stack

Python 3.11+
FastAPI
Uvicorn
NumPy
Gymnasium
PyYAML
Pytest

## Future / Conditional Dependencies

Stable-Baselines3
PyTorch

These are NOT required for initial MVP implementation.

## Policy

Every dependency used by the project must be recorded here.

Do not rely on arbitrary packages merely because they are installed globally.

No dependency is automatically installed during local development.